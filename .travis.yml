---
language: python
dist: xenial
python:
  - 3.6
  - 3.7
sudo: true
install:
  - pip install coverage
script:
  - coverage run --source=hscode/. -m unittest hscode  tests/*.py
after_success:
  - coverage -t %CODECOV_TOKEN
branches:
  only:
    - main
